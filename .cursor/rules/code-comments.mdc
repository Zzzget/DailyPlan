---
description: DailyPlan 代码注释规范：state、类型字段、复杂工具方法与数据转换必须写注释
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# 代码注释规范

编写或修改 DailyPlan 前端代码时，必须为下列位置补充**中文注释**（说明「是什么 / 为什么」，不要复述代码字面意思）。

## 1. 每一个 state

- `useState`、`useReducer`、Context 中的状态、以及自定义 hook 暴露的状态，**每一个**都要有注释。
- 注释写在声明上一行，说明含义、单位/取值范围、以及非显而易见的初始值原因。

```tsx
// ❌ BAD
const [amount, setAmount] = useState(0);

// ✅ GOOD
/** 当前输入的金额，单位：分；未输入时为 0 */
const [amount, setAmount] = useState(0);
```

## 2. TypeScript `type` / `interface` 的每一个字段

- 所有导出或组件内使用的 `type`、`interface`，**每个字段**都要有注释（含可选字段、联合类型成员若语义不直观也要注明）。
- 枚举/字面量联合：注明业务含义，不要只写英文名。

```ts
// ❌ BAD
export interface BillRecord {
  id: string;
  amount: number;
}

// ✅ GOOD
export interface BillRecord {
  /** 账单唯一 ID */
  id: string;
  /** 金额，单位：分；支出为正、收入为负需与业务约定一致 */
  amount: number;
}
```

## 3. 逻辑较复杂的工具方法

- `*Utils`、`helpers`、纯函数工具、自定义 hook 中**分支多、有业务规则、有边界条件**的方法必须写注释。
- 注释至少包含：入参含义、返回值、关键规则/边界（空数组、跨日、时区等）。
- 简单的一行 getter / 透传函数可不写长注释。

## 4. 数据转换

- 对 API 响应、mock、表单值、列表展示之间的 **map / reduce / 聚合 / 格式化 / 单位换算**，必须在转换处或封装函数上写注释。
- 写明：源结构 → 目标结构、字段映射、单位变化、过滤/排序规则。

```ts
// ❌ BAD
const yuan = fen / 100;

// ✅ GOOD
/** 分转元，用于列表展示；保留两位小数在调用方格式化 */
const yuan = fen / 100;
```

## 约束

- 新增代码必须带齐上述注释；改到旧代码时，顺手补全被改动符号的注释。
- 禁止无意义注释（如 `// 设置 amount`）；禁止用注释代替清晰命名。
